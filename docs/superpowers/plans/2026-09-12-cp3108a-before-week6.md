# CP3108A Before Week 6 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use
> superpowers:subagent-driven-development (recommended) or
> superpowers:executing-plans to implement this plan task-by-task. Steps use
> checkbox (`- [ ]`) syntax for tracking.

**Goal:** Complete and publish every CP3108A requirement due before Week 6.

**Architecture:** Keep tutorial evidence in a dedicated fork of the official
JavaFX tutorial and keep each iP extension in its exact course branch. JavaFX
work progresses through five tagged states; iP work progresses through two
independent PRs merged into `master` and tagged at the merge commits.

**Tech Stack:** Java 25, JavaFX 17.0.7, Gradle, JUnit 5, Checkstyle 14.1.0,
GitHub Actions, Git, GitHub CLI.

---

### Task 1: Create the JavaFX tutorial workspace

**Files:**
- Repository: `/Users/zhai/Desktop/CS2103T/javafx-tutorial`
- Modify: `build.gradle`
- Create: `src/main/java/Main.java`
- Create: `src/main/java/Launcher.java`

- [ ] **Step 1: Fork and clone the official starter**

Run:

```bash
gh repo fork se-edu/javafx-tutorial \
  --clone --remote \
  --default-branch-only
```

Expected: `JiayiZhai/javafx-tutorial` exists and `origin` points to that fork.

- [ ] **Step 2: Configure the tutorial exactly for JavaFX**

Add the cross-platform JavaFX 17.0.7 base, controls, FXML, and graphics
dependencies for Windows, macOS, and Linux. Set the application main class to
`Launcher` while retaining the Java 25 toolchain and Shadow plugin.

- [ ] **Step 3: Add the first JavaFX window**

`Main` extends `Application`, creates a `Label("Hello World!")`, places it in a
`Scene`, and shows the supplied `Stage`. `Launcher.main` calls
`Application.launch(Main.class, args)`.

- [ ] **Step 4: Verify and publish Part 1**

Run with Java 25:

```bash
./gradlew clean test shadowJar
```

Commit with subject `Set up the JavaFX application`, add lightweight tag
`Tutorial-Part1`, and push the branch and tag.

### Task 2: Build the JavaFX Duke interface

**Files:**
- Modify: `/Users/zhai/Desktop/CS2103T/javafx-tutorial/src/main/java/Main.java`
- Create: `/Users/zhai/Desktop/CS2103T/javafx-tutorial/src/main/java/DialogBox.java`
- Create: `/Users/zhai/Desktop/CS2103T/javafx-tutorial/src/main/resources/images/DaUser.png`
- Create: `/Users/zhai/Desktop/CS2103T/javafx-tutorial/src/main/resources/images/DaDuke.png`

- [ ] **Step 1: Assemble the tutorial layout**

Create an `AnchorPane` containing a `ScrollPane`, its `VBox` dialog container,
a `TextField`, and a `Send` button. Apply the tutorial's 400 by 600 preferred
size, scroll behavior, and anchors.

- [ ] **Step 2: Add reusable dialog boxes**

Create `DialogBox extends HBox` with a wrapping `Label` and 100 by 100
`ImageView`. Use the two official tutorial avatar assets and display a sample
user message.

- [ ] **Step 3: Verify and publish Part 2**

Run `./gradlew clean test shadowJar`, launch the GUI, confirm the controls and
avatar render, commit with subject `Build the Duke chat interface`, tag
`Tutorial-Part2`, and push.

### Task 3: Connect the GUI to Duke

**Files:**
- Modify: `/Users/zhai/Desktop/CS2103T/javafx-tutorial/src/main/java/Duke.java`
- Modify: `/Users/zhai/Desktop/CS2103T/javafx-tutorial/src/main/java/Main.java`
- Modify: `/Users/zhai/Desktop/CS2103T/javafx-tutorial/src/main/java/DialogBox.java`

- [ ] **Step 1: Add a testable Duke response**

Add `Duke.getResponse(String input)` returning `Duke said: ` followed by the
input. This is the tutorial's minimal stand-in for the final command engine.

- [ ] **Step 2: Handle keyboard and button input**

Wire both `TextField.setOnAction` and `Button.setOnMouseClicked` to a shared
handler. Add the user dialog followed by Duke's response, clear the input, and
keep the scroll pane pinned to the newest dialog.

- [ ] **Step 3: Distinguish both speakers**

Add `getUserDialog` and `getDukeDialog` factories. Reverse the children and
left-align only Duke replies so the asymmetric chat direction is visible.

- [ ] **Step 4: Verify and publish Part 3**

Build, launch, submit text once with Enter and once with the Send button,
commit with subject `Connect Duke to the chat interface`, tag
`Tutorial-Part3`, and push.

### Task 4: Refactor the tutorial GUI to FXML

**Files:**
- Create: `/Users/zhai/Desktop/CS2103T/javafx-tutorial/src/main/java/MainWindow.java`
- Modify: `/Users/zhai/Desktop/CS2103T/javafx-tutorial/src/main/java/Main.java`
- Modify: `/Users/zhai/Desktop/CS2103T/javafx-tutorial/src/main/java/DialogBox.java`
- Create: `/Users/zhai/Desktop/CS2103T/javafx-tutorial/src/main/resources/view/MainWindow.fxml`
- Create: `/Users/zhai/Desktop/CS2103T/javafx-tutorial/src/main/resources/view/DialogBox.fxml`

- [ ] **Step 1: Move layout into `MainWindow.fxml`**

Declare the `AnchorPane`, `ScrollPane`, `VBox`, `TextField`, and `Button` in
FXML. Bind `fx:id` values to `MainWindow` fields and both input actions to
`#handleUserInput`.

- [ ] **Step 2: Move dialog layout into `DialogBox.fxml`**

Use `fx:root type="javafx.scene.layout.HBox"`, an `ImageView` named
`displayPicture`, and a wrapping `Label` named `dialog`.

- [ ] **Step 3: Separate controller and application responsibilities**

`MainWindow` owns input handling and receives a `Duke` instance through
`setDuke`. `DialogBox` loads its own FXML. `Main.start` loads
`MainWindow.fxml`, supplies Duke to its controller, and displays the scene.

- [ ] **Step 4: Verify and publish Part 4**

Build and launch the FXML version, commit with subject
`Refactor the chat interface to FXML`, tag `Tutorial-Part4`, and push.

### Task 5: Complete more than half of the Part 5 tweaks

**Files:**
- Modify: `/Users/zhai/Desktop/CS2103T/javafx-tutorial/src/main/java/Main.java`
- Modify: `/Users/zhai/Desktop/CS2103T/javafx-tutorial/src/main/java/DialogBox.java`
- Modify: `/Users/zhai/Desktop/CS2103T/javafx-tutorial/src/main/resources/view/MainWindow.fxml`
- Modify: `/Users/zhai/Desktop/CS2103T/javafx-tutorial/src/main/resources/view/DialogBox.fxml`
- Create: `/Users/zhai/Desktop/CS2103T/javafx-tutorial/src/main/resources/css/main.css`
- Create: `/Users/zhai/Desktop/CS2103T/javafx-tutorial/src/main/resources/css/dialog-box.css`

- [ ] **Step 1: Make the window responsive**

Anchor the text field to the left, bottom, and send button; anchor the button
to the right and bottom; anchor the scroll pane to all three upper sides. Set a
minimum window size while allowing resizing.

- [ ] **Step 2: Add CSS and speaker-specific bubbles**

Link `main.css` from `MainWindow.fxml` and `dialog-box.css` from
`DialogBox.fxml`. Style the root, scroll pane, input, button states, labels,
borders, padding, rounded corners, and avatar shadows. Add `reply-label` when
flipping Duke's dialog.

- [ ] **Step 3: Add usability polish**

Set the prompt text to `Type here...`, retain automatic scrolling, wrap long
messages, and keep speaker images consistently sized.

- [ ] **Step 4: Verify and publish Part 5**

Build and inspect the GUI at minimum and larger sizes. Confirm Enter, Send,
long text wrapping, auto-scroll, hover, pressed styling, and both dialog
directions. Commit with subject `Polish the responsive JavaFX interface`, tag
`Tutorial-Part5`, and push all five tags.

### Task 6: Add Checkstyle to the iP

**Files:**
- Modify: `/Users/zhai/Desktop/CS2103T/ip/build.gradle`
- Create: `/Users/zhai/Desktop/CS2103T/ip/config/checkstyle/checkstyle.xml`
- Create: `/Users/zhai/Desktop/CS2103T/ip/config/checkstyle/suppressions.xml`
- Modify: Java files reported by Checkstyle only when needed

- [ ] **Step 1: Create the exact increment branch**

Update `master` from `origin/master`, then create `branch-A-Checkstyle`.

- [ ] **Step 2: Install the course configuration**

Add Gradle's `checkstyle` plugin, set `toolVersion = '14.1.0'`, and reuse the
current AddressBook-Level3 `checkstyle.xml` and `suppressions.xml` files.

- [ ] **Step 3: Observe and fix violations**

Run:

```bash
./gradlew checkstyleMain checkstyleTest
```

Inspect every report and fix source style without altering behavior. Re-run
`./gradlew clean check` and require a zero exit status.

- [ ] **Step 4: Publish course evidence**

Commit with subject `Add automated Checkstyle checks`, push the branch, create
and merge a PR, push the merged branch tip, add lightweight tag
`A-Checkstyle` to the merge commit, and push the tag.

### Task 7: Add GitHub Actions CI to the iP

**Files:**
- Create: `/Users/zhai/Desktop/CS2103T/ip/.github/workflows/gradle.yml`

- [ ] **Step 1: Create the exact increment branch**

From the updated `master`, create `branch-A-CI`.

- [ ] **Step 2: Add the recommended workflow**

Use triggers `push` and `pull_request`. Test on `ubuntu-latest`,
`macos-latest`, and `windows-latest`. Use `actions/checkout@v7`,
`gradle/actions/wrapper-validation@v6`, and `actions/setup-java@v5` with Zulu
Java 25 plus JavaFX. Finish with `./gradlew check`.

- [ ] **Step 3: Verify locally and on GitHub**

Parse the YAML, run `./gradlew clean check` locally, push the branch, and wait
for all matrix jobs to complete successfully.

- [ ] **Step 4: Publish course evidence**

Create and merge the PR, push the merged branch tip, add lightweight tag
`A-CI` to the merge commit, and push the tag. Confirm both tags and branches
appear on GitHub.

### Task 8: Final Week 1–5 audit

**Files:**
- No source changes expected

- [ ] **Step 1: Audit Git-Mastery**

Run `gitmastery progress show` and confirm all 32 compulsory exercises remain
`Completed` and progress sync remains enabled.

- [ ] **Step 2: Audit JavaFX evidence**

Confirm the fork contains the five tags, each points to the corresponding
tutorial progression, and the final Gradle build passes under Java 25.

- [ ] **Step 3: Audit iP evidence**

Confirm both PRs, merge commits, exact branches, fully merged remote branch
tips, exact lightweight tags, Gradle tests, Checkstyle, and CI runs.

- [ ] **Step 4: Report the Week 6 boundary**

Report Week 1–5 complete. Explicitly leave `A-BetterGui`, `A-Personality`,
`A-MoreErrorHandling`, `A-MoreTesting`, and AB3 tutorials for their Week 6–7
window rather than claiming them early.
